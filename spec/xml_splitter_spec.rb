require "spec_helper"

describe XmlSplitter do
  it "has a version number" do
    expect(XmlSplitter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
